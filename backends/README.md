# Backends — UNIMPLEMENTED

M0 ne fournit aucun provider. `vulkan/`, `dx12/` et `opengl/` réservent leurs
frontières futures ; aucun SDK n'est requis. Le nom dans la CLI est seulement
un candidat, pas une preuve de support. M1/M3 bloqués par le gate M0.