# Validation M0 — gate ouvert, M1 bloqué

Date : **2026-09-18 UTC**. État : **M0 livré pour vérification, PAS déclaré achevé**.
La compilation croisée ne prouve pas l'exécution sur Windows. Une exécution
QEMU ne remplace pas un runner Linux x64 natif. Aucun run GitHub Actions accessible
n'a été exécuté dans cette session ; aucune URL de CI « verte » n'est inventée.

## Mesures locales OBSERVED

Hôte : Linux aarch64, Debian 12, glibc 2.36, Clang/LLVM **14.0.6**, CMake **3.25.1**,
Ninja **1.11.1**, libstdc++ GCC **12.2.0**. Sources : arbre de travail M0 de cette
livraison ; commit distant associé **UNKNOWN**. Aucun SDK graphique ni dump présent.

| Configuration | Build | Exécution | Résultat |
|---|---|---|---|
| ARM64 Debug, dérogation explicite | OBSERVED PASS, warnings en erreurs | native sur cet hôte | 26/26 CTests |
| ARM64 Release | OBSERVED PASS | native sur cet hôte | 26/26 CTests |
| Linux x64 Debug, cross Clang 14.0.6 + GCC 12.2 | OBSERVED PASS, ELF64 AMD x86-64 vérifié | QEMU user 7.2 + glibc x64 | 26/26 CTests |
| Linux x64 Release, même chaîne | OBSERVED PASS | QEMU user | 26/26 CTests |
| ARM64 coverage | OBSERVED PASS | LLVM instrumentation | 26/26 CTests ; rapport sans diagnostic LLVM |
| ARM64 ASan + UBSan | OBSERVED PASS | detect_leaks/halts activés | 26/26 CTests, aucun diagnostic dans ces cas |
| Windows x64 cross LLVM-MinGW 20240619 / Clang 18.1.8 Debug | OBSERVED PASS, COFF/PE32+ AMD64 vérifié | NON EXÉCUTÉ | tests Windows UNKNOWN |
| Windows x64 cross, Release | OBSERVED PASS | NON EXÉCUTÉ | tests Windows UNKNOWN |

Ces vérifications ARM64 ne créent pas une nouvelle plateforme officiellement
supportée. QEMU est un outil de test **hôte x64**, sans rapport avec une émulation PowerPC.

## Protocole / artefacts locaux

Commandes exactes reproductibles : [build.md](../build.md). Build/test natifs via
presets avec `-DMANMENMI_ALLOW_UNSUPPORTED_HOST=ON` sur cet hôte. CTests croisés
utilisent `CMAKE_CROSSCOMPILING_EMULATOR`, y compris les tests de CLI CMake.

- `build/<configuration>/Testing/Temporary/LastTest.log` : journaux des 26 tests.
- `build/<configuration>/results.xml` : résultat JUnit des commandes avec `--output-junit`.
- `build/linux-clang-coverage/coverage.txt` : mesure LLVM.
- `build/windows-x64-cross[-release]/manmenmi_probe.exe` : objet Windows construit,
  **pas** preuve de lancement ni paquet de distribution.

Les artefacts de build sont ignorés par Git. Les runs CI futurs archiveront leur
propre JUnit/log/cache/coverage pour une preuve attachée à un commit.

### Vérification indépendante et réserves traitées

**OBSERVED :** le contrôle indépendant a reconstruit puis réexécuté les six
configurations exécutables (26/26 chacune) après le déplacement vers `result.cpp`,
reconstruit les deux configurations Windows et vérifié des cas CLI supplémentaires.
Rapport local : `/app/test_reports/iteration_1.json`. Aucun défaut produit signalé.

Deux réserves d'outillage ont ensuite été levées, sans modification du produit :

- **OBSERVED :** `actionlint 1.7.7 .github/workflows/ci.yml` retourne **0**, aucun
  diagnostic. Le programme manquait pendant le contrôle indépendant ; installé
  ensuite depuis la release épinglée. C'est une validation **statique**, pas une CI exécutée.
- **OBSERVED :** configuration avec LLVM-MinGW `i686-w64-mingw32-clang++` 18.1.8,
  `CMAKE_SYSTEM_NAME=Windows`, `CMAKE_SYSTEM_PROCESSOR=i686` et dérogation
  d'architecture activée : rejet effectif **« MANMENMI requires a 64-bit toolchain. »**.
  Le linker 32 bits de cette distribution est complet. Log local :
  `build/reject-windows-32.log`. Aucun support 32 bits ajouté.

La seule réserve bloquant le gate global demeure l'exécution CI **native** sur
les deux plateformes cibles, avec preuves liées à un commit.

### Couverture de sources, pas de compatibilité

**OBSERVED :** 96,25 % des lignes (257/267), 94,50 % des branches (189/200),
96,79 % des régions (241/249), 100 % des fonctions reportées (29/29), après
exclusion des tests et fichiers système. Outil : LLVM 14.0.6, profils frais,
binaire CLI + quatre exécutables unitaires.

L'avertissement initial de profils à hash nul pour deux fonctions inline a été
diagnostiqué et supprimé en déplaçant ces fonctions dans `result.cpp` ; aucun
avertissement n'est filtré. Le script échoue si LLVM émet un diagnostic.
Les lignes non couvertes comprennent les chemins de récupération d'erreur
interne/exception et valeurs enum hors contrat ; cela ne constitue pas un test
exhaustif d'erreurs système. **Couverture GX2 : 0 appel**, corpus GPU inexistant.

## Gate de réussite strict

- [x] README strict, MIT, politique de dépendances et notices.
- [x] Sources initiales et marqueurs DOCUMENTED/OBSERVED/INFERRED/UNKNOWN.
- [x] Arborescence native, configuration hors ligne sans dump.
- [x] Interfaces fondamentales sans types natifs ; aucune implémentation M1+.
- [x] CMake/Ninja et tests autonomes Debug/Release observés localement.
- [x] Compilation croisée Linux x64 et Windows x64 observée.
- [x] Workflow six configurations Windows/Linux défini, actions épinglées.
- [ ] **P0 :** Linux x64 natif CI Debug/Release/coverage/sanitizers réellement passés.
- [ ] **P0 :** Windows x64 natif CI Debug/Release réellement passés.
- [ ] **P0 :** URL/commit/artefacts du run consignés et tout échec corrigé.

**Décision : ne pas commencer M1.** Aucun backend graphique, GX2, Cafe ou parser
de shader ne doit être ajouté pour contourner un manque de preuve M0.

## Suite après satisfaction du gate seulement

M1 : recherche/pin SDL3, pont privé de présentation, test fenêtre/device/present
et lifecycle par backend réellement disponible. M2 : ressources et IR portable.
P1 de reproductibilité : hashes prévalidés des archives et image/outils système
plus strictement figés. Pas de promesse d'égalité binaire dans l'état actuel.