# Core — fondation hôte M0

Implémenté : `Result<T>`/erreurs, logger, config v1/CLI, inventaire d'état et
plan de candidats. `create_runtime` échoue explicitement : runtime concret
UNIMPLEMENTED. Aucun service Cafe OS n'est simulé.
Les sous-dossiers memory/timing/threading sont PLANNED, pas des moteurs existants.