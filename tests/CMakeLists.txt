file(MAKE_DIRECTORY "${CMAKE_CURRENT_BINARY_DIR}/fixtures with spaces")
set(fixture "${CMAKE_CURRENT_BINARY_DIR}/fixtures with spaces/valid.conf")
file(WRITE "${fixture}" "schema_version=1\nbackend=opengl\nallow_fallback=true\nlog_level=debug\n")
file(WRITE "${CMAKE_CURRENT_BINARY_DIR}/invalid.conf" "schema_version=1\nbackend=typo\n")
foreach(suite IN ITEMS core config log contracts graphics window)
  add_executable(manmenmi_${suite}_tests ${suite}_tests.cpp)
  target_link_libraries(manmenmi_${suite}_tests PRIVATE manmenmi::core manmenmi::window manmenmi::graphics manmenmi_options)
  if(suite STREQUAL "window")
    target_link_libraries(manmenmi_${suite}_tests PRIVATE manmenmi_window_native SDL3::SDL3)
  endif()
  if(suite STREQUAL "graphics")
    target_link_libraries(manmenmi_${suite}_tests PRIVATE manmenmi_window_native SDL3::SDL3)
  endif()
  if(suite STREQUAL "config")
    add_test(NAME unit.${suite} COMMAND manmenmi_${suite}_tests "${fixture}")
  else()
    add_test(NAME unit.${suite} COMMAND manmenmi_${suite}_tests)
  endif()
  if(suite STREQUAL "window")
    set_tests_properties(unit.${suite} PROPERTIES LABELS "unit;foundation;window" TIMEOUT 30 ENVIRONMENT "SDL_VIDEODRIVER=dummy")
  else()
    set_tests_properties(unit.${suite} PROPERTIES LABELS "unit;foundation" TIMEOUT 30)
  endif()
endforeach()

add_executable(manmenmi_runtime_integration_tests graphics_runtime_integration.cpp)
target_link_libraries(manmenmi_runtime_integration_tests PRIVATE manmenmi::core manmenmi::window manmenmi::graphics manmenmi_window_native manmenmi_options SDL3::SDL3)
add_test(NAME runtime.integration_m13 COMMAND manmenmi_runtime_integration_tests)
set_tests_properties(runtime.integration_m13 PROPERTIES LABELS "runtime;integration;m1.3" TIMEOUT 120)

add_executable(manmenmi_m2_command_runtime_tests m2_command_runtime.cpp)
target_link_libraries(manmenmi_m2_command_runtime_tests PRIVATE manmenmi::core manmenmi::window manmenmi::graphics manmenmi_window_native manmenmi_options SDL3::SDL3)
add_test(NAME runtime.integration_m22 COMMAND manmenmi_m2_command_runtime_tests)
set_tests_properties(runtime.integration_m22 PROPERTIES LABELS "runtime;integration;m2.2" TIMEOUT 120)

add_executable(manmenmi_m3_triangle_runtime_tests m3_triangle_runtime.cpp)
target_link_libraries(manmenmi_m3_triangle_runtime_tests PRIVATE manmenmi::core manmenmi::window manmenmi::graphics manmenmi_window_native manmenmi_options SDL3::SDL3)
add_test(NAME runtime.integration_m3 COMMAND manmenmi_m3_triangle_runtime_tests)
set_tests_properties(runtime.integration_m3 PROPERTIES LABELS "runtime;integration;m3" TIMEOUT 120)

add_executable(manmenmi_gx2_tests gx2_tests.cpp)
target_link_libraries(manmenmi_gx2_tests PRIVATE manmenmi::gx2 manmenmi_options)
add_test(NAME unit.gx2 COMMAND manmenmi_gx2_tests)
set_tests_properties(unit.gx2 PROPERTIES LABELS "unit;gx2" TIMEOUT 30)

add_executable(manmenmi_gx2_runtime_tests gx2_runtime.cpp)
target_link_libraries(manmenmi_gx2_runtime_tests PRIVATE manmenmi::gx2 manmenmi::core manmenmi::window manmenmi::graphics manmenmi_window_native manmenmi_options SDL3::SDL3)
add_test(NAME integration.gx2 COMMAND manmenmi_gx2_runtime_tests)
set_tests_properties(integration.gx2 PROPERTIES LABELS "integration;gx2" TIMEOUT 120)
add_test(NAME runtime.integration_m4 COMMAND manmenmi_gx2_runtime_tests)
set_tests_properties(runtime.integration_m4 PROPERTIES LABELS "runtime;integration;m4" TIMEOUT 120)

add_executable(manmenmi_shader_ir_tests shader_ir_tests.cpp)
target_link_libraries(manmenmi_shader_ir_tests PRIVATE manmenmi::core manmenmi_options)
add_test(NAME unit.shader_ir COMMAND manmenmi_shader_ir_tests)
set_tests_properties(unit.shader_ir PROPERTIES LABELS "unit;shader;m6" TIMEOUT 30)

if(WIN32)
  add_executable(manmenmi_shader_translation_tests shader_translation_tests.cpp)
  target_link_libraries(manmenmi_shader_translation_tests PRIVATE manmenmi::core manmenmi_options d3dcompiler)
  add_test(NAME integration.shader_translation COMMAND manmenmi_shader_translation_tests)
  set_tests_properties(integration.shader_translation PROPERTIES LABELS "integration;shader;m6" TIMEOUT 30)
endif()

add_executable(manmenmi_texture_tests texture_tests.cpp)
target_link_libraries(manmenmi_texture_tests PRIVATE manmenmi::core manmenmi::graphics manmenmi_options)
add_test(NAME unit.texture COMMAND manmenmi_texture_tests)
set_tests_properties(unit.texture PROPERTIES LABELS "unit;texture;m7" TIMEOUT 30)

add_executable(manmenmi_texture_integration_tests texture_integration.cpp)
target_link_libraries(manmenmi_texture_integration_tests PRIVATE manmenmi::core manmenmi::graphics manmenmi_window_native manmenmi_options SDL3::SDL3)
add_test(NAME integration.texture COMMAND manmenmi_texture_integration_tests)
set_tests_properties(integration.texture PROPERTIES LABELS "integration;texture;m7" TIMEOUT 60)

add_executable(manmenmi_m7_texture_runtime_tests m7_texture_runtime.cpp)
target_link_libraries(manmenmi_m7_texture_runtime_tests PRIVATE manmenmi::core manmenmi::window manmenmi::graphics manmenmi_window_native manmenmi_options SDL3::SDL3)
add_test(NAME runtime.integration_m7 COMMAND manmenmi_m7_texture_runtime_tests)
set_tests_properties(runtime.integration_m7 PROPERTIES LABELS "runtime;integration;m7" TIMEOUT 120)

add_executable(manmenmi_gx2_texture_tests gx2_texture_tests.cpp)
target_link_libraries(manmenmi_gx2_texture_tests PRIVATE manmenmi::core manmenmi::gx2 manmenmi_options)
add_test(NAME unit.gx2_texture COMMAND manmenmi_gx2_texture_tests)
set_tests_properties(unit.gx2_texture PROPERTIES LABELS "unit;gx2;texture;m8" TIMEOUT 30)

add_executable(manmenmi_gx2_texture_integration_tests gx2_texture_integration.cpp)
target_link_libraries(manmenmi_gx2_texture_integration_tests PRIVATE manmenmi::core manmenmi::graphics manmenmi::gx2 manmenmi_options)
add_test(NAME integration.gx2_texture COMMAND manmenmi_gx2_texture_integration_tests)
set_tests_properties(integration.gx2_texture PROPERTIES LABELS "integration;gx2;texture;m8" TIMEOUT 120)

add_executable(manmenmi_m8_gx2_texture_runtime_tests m8_gx2_texture_runtime.cpp)
target_link_libraries(manmenmi_m8_gx2_texture_runtime_tests PRIVATE manmenmi::core manmenmi::window manmenmi::graphics manmenmi::gx2 manmenmi_window_native manmenmi_options SDL3::SDL3)
add_test(NAME runtime.integration_m8 COMMAND manmenmi_m8_gx2_texture_runtime_tests)
set_tests_properties(runtime.integration_m8 PROPERTIES LABELS "runtime;integration;m8" TIMEOUT 120)

# Compile each public header in isolation, without relying on transitive includes.
set(public_headers core/result.hpp core/backend.hpp core/log.hpp core/config.hpp core/runtime.hpp window/window.hpp graphics/graphics.hpp gx2/gx2.hpp)
foreach(header IN LISTS public_headers)
  string(REPLACE "/" "_" filename "${header}")
  set(source "${CMAKE_CURRENT_BINARY_DIR}/header_${filename}.cpp")
  file(WRITE "${source}" "#include <manmenmi/${header}>\n")
  list(APPEND header_sources "${source}")
endforeach()
add_library(manmenmi_header_contracts OBJECT ${header_sources})
target_link_libraries(manmenmi_header_contracts PRIVATE manmenmi::core manmenmi::window manmenmi::graphics manmenmi::gx2 manmenmi_options)

function(cli_case name exit_code match forbid)
  add_test(NAME cli.${name} COMMAND "${CMAKE_COMMAND}"
    "-DPROGRAM=$<TARGET_FILE:manmenmi_probe>" "-DEMULATOR=${CMAKE_CROSSCOMPILING_EMULATOR}"
    "-DARGUMENTS=${ARGN}" "-DEXPECT_EXIT=${exit_code}" "-DEXPECT_TEXT=${match}" "-DFORBID_TEXT=${forbid}"
    -P "${CMAKE_CURRENT_SOURCE_DIR}/assert_cli.cmake")
  set_tests_properties(cli.${name} PROPERTIES LABELS "cli;foundation" TIMEOUT 30)
endfunction()
cli_case(help 0 "Usage: manmenmi_probe" "" --help)
cli_case(version 0 "MANMENMI 0.1.0 M0" "" --version)
cli_case(default_config 0 "allow_fallback=false" "" --check-config)
cli_case(file_config 0 "backend=opengl" "" "--config=${fixture}" --check-config)
cli_case(cli_precedence 0 "backend=dx12" "" --backend=dx12 "--config=${fixture}" --check-config)
cli_case(disable_file_fallback 0 "allow_fallback=false" "" "--config=${fixture}" --no-fallback --check-config)
cli_case(backend_inventory 0 "vulkan: UNIMPLEMENTED.*dx12: UNIMPLEMENTED.*opengl: UNIMPLEMENTED" "AVAILABLE" --list-backends)
cli_case(default_probe 3 "UNIMPLEMENTED" "explicit fallback")
cli_case(auto_strict 3 "backend=vulkan state=UNIMPLEMENTED" "explicit fallback" --backend=auto)
cli_case(vulkan_unimplemented 3 "backend=vulkan state=UNIMPLEMENTED" "explicit fallback" --backend=vulkan)
cli_case(dx12_unimplemented 3 "backend=dx12 state=UNIMPLEMENTED" "explicit fallback" --backend=dx12)
cli_case(opengl_unimplemented 3 "backend=opengl state=UNIMPLEMENTED" "explicit fallback" --backend=opengl)
cli_case(explicit_fallback 3 "explicit fallback attempt backend=dx12.*explicit fallback attempt backend=opengl" "" --backend=auto --allow-fallback --log-level=error)
cli_case(invalid_backend 2 "INVALID_ARGUMENT" "UNIMPLEMENTED" --backend=metal)
cli_case(unknown_option 2 "unknown option" "" --typo)
cli_case(missing_value 2 "nonempty =value" "" --backend)
cli_case(duplicate_option 2 "duplicate option" "" --backend=vulkan --backend=opengl)
cli_case(conflicting_fallback 2 "conflicting fallback" "" --allow-fallback --no-fallback)
cli_case(conflicting_commands 2 "choose only one command" "" --help --probe)
cli_case(missing_config 4 "IO_ERROR" "" "--config=${fixture}.missing")
cli_case(invalid_config 2 "config line 2" "" "--config=${CMAKE_CURRENT_BINARY_DIR}/invalid.conf")
add_test(NAME architecture.no_native_types COMMAND "${CMAKE_COMMAND}" "-DROOT=${PROJECT_SOURCE_DIR}" -P "${CMAKE_CURRENT_SOURCE_DIR}/check_public_headers.cmake")
set_tests_properties(architecture.no_native_types PROPERTIES LABELS "architecture;foundation" TIMEOUT 30)

# Opt-in negative workflow run only: normal M0 suite stays at exactly 26 tests.
option(MANMENMI_CI_INJECT_FAILURE "Add a deliberately failing sentinel for CI propagation checks" OFF)
if(MANMENMI_CI_INJECT_FAILURE)
  add_test(NAME sentinel.forced_failure COMMAND "${CMAKE_COMMAND}" -E false)
  set_tests_properties(sentinel.forced_failure PROPERTIES LABELS "ci-negative" TIMEOUT 10)
endif()
